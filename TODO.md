# TODO List for Adding Root Route to Proxy

- [x] Edit proxy.py to add handling for the root URL '/' by modifying the route to include defaults for empty path
- [x] Test the changes by verifying requests to '/' no longer return 404 and are properly proxied
